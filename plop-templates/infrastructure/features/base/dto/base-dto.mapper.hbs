import { AutoMapper, IMapper } from "@/lib/ts-utilities";
import { {{classify name}} } from "@/core/domain/{{dasherize group}}";
import { {{classify name}}Dto } from "./{{dasherize name}}.dto";


export class {{classify name}}DtoMapper {
  mapFrom(object: {{classify name}}): {{classify name}}Dto {
    return new {{classify name}}Dto(object);
  }

  mapTo(object: {{classify name}}Dto): {{classify name}} {
    return new {{classify name}}(object);
  }
}
